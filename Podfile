# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SafaricomHackathon' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SafaricomHackathon
  pod 'Alamofire', '~> 4.2.0'
  pod 'SDWebImage', '~> 4.0'
  pod 'ProgressHUD'
  target 'SafaricomHackathonTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SafaricomHackathonUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
